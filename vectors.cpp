#include <iostream>
#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main() {

    vector<int> v;

    // v.push_back(10);
    // v.push_back(20);
    // v.push_back(30);
    // v.push_back(40);
    // v.push_back(50);
    // v.push_back(60);
    // v.pop_back();
    // cout<<"size: "<<v.size()<<endl;
    // cout<<v.at(4)<<endl;
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout<<v[i]<<endl;
    // }
    

//    for (int i = 0; i < 100; i++)
//    {
//        v.push_back(i);
//        cout<<v.capacity()<<endl;
//    }
   

    // use v[] only to update and excess value and not to insert new value in vector.
    //to insert new value use v.push_back() .
    //use v.at() to access value as it will give error if we r going out  of range while
    //v[] will return garbage value out of range ;
    //use it in loop since we know the range there;



    return 0;
}